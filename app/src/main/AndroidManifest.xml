<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ljoias"
        tools:targetApi="31">

        <!-- Declaração da MainActivity -->
        <activity
            android:name="com.example.myapplication.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaração da CarrinhoActivity -->
        <activity
            android:name="com.example.myapplication.CarrinhoActivity"
            android:exported="true" />

        <!-- Declaração da ContaActivity -->
        <activity
            android:name="com.example.myapplication.ContaActivity"
            android:exported="true" />

        <!-- Declaração da CadastroActivity -->
        <activity
            android:name="com.example.myapplication.CadastroActivity"
            android:exported="true" />

        <!-- Declaração da PulseirasActivity -->
        <activity
            android:name=".PulseirasActivity"
            android:exported="true" />



    </application>

</manifest>
